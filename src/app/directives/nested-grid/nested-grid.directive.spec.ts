import { NestedGridDirective } from './nested-grid.directive';

describe('NestedGridDirective', () => {
  it('should create an instance', () => {
    // const directive = new NestedGridDirective();
    // expect(directive).toBeTruthy();
  });
});
